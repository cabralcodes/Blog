<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>{{postagem.data}}</small>
        <br>
        <small>{{categoria}}</small>
        <br>
        {{postagem.conteudo}}
    </div>
</div>
